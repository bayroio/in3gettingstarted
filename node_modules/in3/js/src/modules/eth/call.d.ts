import { AccountProof } from '../../types/types';
/** executes a transaction-call to a smart contract */
export declare function executeCall(args: {
    to: string;
    data: string;
    value?: string;
    from?: string;
}, accounts: {
    [adr: string]: AccountProof;
}, block: Buffer): Promise<Buffer>;
